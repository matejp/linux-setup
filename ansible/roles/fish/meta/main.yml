---
galaxy_info:
  role_name: fish
  description: Install and configure fish shell
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: EL
      versions: ["all"]
    - name: Debian
      versions: ["all"]
    - name: ArchLinux
      versions: ["all"]
  galaxy_tags:
    - fish
    - shell
    - dotfiles

dependencies:
  - role: distro_facts
    tags: always
