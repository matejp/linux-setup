---
galaxy_info:
  role_name: packages_base
  description: Install base system packages
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: EL
      versions: ["all"]
    - name: Debian
      versions: ["all"]
    - name: ArchLinux
      versions: ["all"]
  galaxy_tags:
    - packages
    - base
    - system

dependencies:
  - role: distro_facts
    tags: always
